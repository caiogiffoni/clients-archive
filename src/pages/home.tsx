import { Container, Typography } from "@mui/material";
import { Box } from "@mui/system";

export const Home = () => {
  return (
    <Box>
      <Typography variant="h3" sx={{ p: 3 }}>
        Bem vindo, João!
      </Typography>
    </Box>
  );
};
